export enum PropertyType {
    NUMBER = 'number',
    STRING = 'string',
    BIGINT = 'bigint',
    BOOLEAN = 'boolean',
    SYMBOL = 'symbol',
    UNDEFINED = 'undefined',
    OBJECT = 'object',
    FUNCTION = 'function'
}